power := 4;
base := 2;
answer := 1;
while (power) do
    if (power - power / 2 * 2) then
        answer := answer * base;
        power := power - 1;
    else
        base := base * base;
        power := power / 2;
    fi
done
